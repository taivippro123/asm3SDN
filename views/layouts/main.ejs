<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Football Players Management</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f6fa;
        }
        .navbar {
            background: #23272f;
            color: #fff;
            padding: 0;
            box-shadow: 0 2px 8px #0002;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        .navbar .container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 24px;
            height: 60px;
        }
        .navbar-brand {
            font-weight: bold;
            color: #fff;
            text-decoration: none;
            font-size: 1.5em;
            letter-spacing: 1px;
        }
        .navbar-nav {
            list-style: none;
            display: flex;
            gap: 20px;
            margin: 0;
            padding: 0;
        }
        .navbar-nav li { display: inline; }
        .navbar-nav a {
            color: #cfd8dc;
            text-decoration: none;
            padding: 6px 14px;
            border-radius: 4px;
            font-size: 1em;
            transition: background 0.2s, color 0.2s;
        }
        .navbar-nav a:hover, .navbar-nav a.active {
            background: #374151;
            color: #fff;
        }
        .main-content {
            max-width: 1100px;
            margin: 36px auto 0 auto;
            background: #fff;
            padding: 32px 28px 32px 28px;
            border-radius: 14px;
            box-shadow: 0 4px 24px #0001;
            min-height: 70vh;
        }
        .alert {
            padding: 12px 20px;
            border-radius: 6px;
            margin-bottom: 18px;
            font-size: 1.08em;
            font-weight: 500;
            letter-spacing: 0.01em;
        }
        .alert-success {
            background: #e8f9e5;
            color: #217a2c;
            border: 1px solid #b2e5b2;
        }
        .alert-danger {
            background: #ffeaea;
            color: #a11a1a;
            border: 1px solid #e5b2b2;
        }
        @media (max-width: 700px) {
            .main-content { padding: 12px 4vw; }
            .navbar .container { flex-direction: column; height: auto; padding: 0 8px; }
            .navbar-nav { gap: 8px; }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a class="navbar-brand" href="/">Football Players</a>
            <ul class="navbar-nav">
                <li><a href="/" class="<%= (typeof page !== 'undefined' && page === 'home') ? 'active' : '' %>">Home</a></li>
                <li><a href="/players" class="<%= (typeof page !== 'undefined' && page === 'players') ? 'active' : '' %>">Players</a></li>
                <li><a href="/teams" class="<%= (typeof page !== 'undefined' && page === 'teams') ? 'active' : '' %>">Teams</a></li>
                <% if (user) { %>
                    <% if (user.isAdmin) { %>
                        <li><a href="/accounts" class="<%= (typeof page !== 'undefined' && page === 'accounts') ? 'active' : '' %>">Members</a></li>
                    <% } %>
                    <li><a href="/auth/profile" class="<%= (typeof page !== 'undefined' && page === 'profile') ? 'active' : '' %>">Profile</a></li>
                    <li><a href="/auth/logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/auth/login" class="<%= (typeof page !== 'undefined' && page === 'login') ? 'active' : '' %>">Login</a></li>
                    <li><a href="/auth/register" class="<%= (typeof page !== 'undefined' && page === 'register') ? 'active' : '' %>">Register</a></li>
                <% } %>
            </ul>
        </div>
    </nav>
    <div class="main-content">
        <% if (success_msg && success_msg.length && success_msg[0]) { %>
            <div class="alert alert-success"><%= success_msg[0] %></div>
        <% } %>
        <% if (error_msg && error_msg.length && error_msg[0]) { %>
            <div class="alert alert-danger"><%= error_msg[0] %></div>
        <% } %>
        <% if (error && error.length && error[0]) { %>
            <div class="alert alert-danger"><%= error[0] %></div>
        <% } %>
        <%- body %>
    </div>
</body>
</html> 